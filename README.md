# CARELINK — Care & Healthcare Workforce Operating Platform

돌봄·의료 인력의 **확보 → 검증 → 교육 → 자격 경로 → 매칭 → 배치 → 근무 운영 → 근속**을
하나의 시스템으로 관리하는 플랫폼. 병원 간병 서비스 운영이 여기에 붙습니다.

작업 규칙은 **`CLAUDE.md`를 먼저 읽으세요.** 이 파일은 자료 현황과 진행 상태만 관리합니다.

---

## 1. 현재 상태

**Phase 1 (설계 자료 취합) 진행 중.** 코드베이스는 아직 없습니다.
`CLAUDE.md` §8 개발 순서의 1단계(스키마 마이그레이션 + 시드) 착수 전이며,
**화면 사양의 단일 출처인 `wireframe/index.html`의 `SCREENS` 배열이 아직 저장소에 없습니다.**

## 2. 자료 현황

### 들어와 있는 것

| 경로 | 내용 |
|---|---|
| `CLAUDE.md` | 작업 규칙 · 절대 규칙 15종 · 도메인 금지 20종 · 개발 순서 · DoD |
| `docs/01_사업제안서.md` | 사업 구조, 공급 전략, 수익 모델, 착수 전 확정 사항 |
| `docs/03_schema.sql` | PostgreSQL 60+ 테이블 + ENUM + 개발용 시드 (산업/트랙/비자/교대/보존정책) |
| `docs/11_개인정보_보안_및_컴플라이언스.md` | 민감정보, 국외이전, scope 매트릭스, 보존·파기, 보안 요구사항 |
| `design/README.md` | 41개 화면 디자인 핸드오프 — 토큰, 신규 컴포넌트 5종, 화면별 제약 |

### 아직 없는 것

`CLAUDE.md` §0이 참조하지만 저장소에 없는 문서입니다. 없으면 해당 영역 구현을 시작할 수 없습니다.

| 경로 | 왜 필요한가 | 막히는 작업 |
|---|---|---|
| **`wireframe/index.html`** (`SCREENS` 배열) | **화면 사양의 단일 출처 = PRD.** route/api/data/states/notes | **모든 화면 구현** |
| `docs/02_ARCHITECTURE.md` | 모듈 경계, 권한 모델, 상태머신, 매칭 엔진 규칙 | API 설계 전체 |
| `docs/04_고용모델_및_버티컬확장.md` | `EngagementStrategy` 3종, 트랙·산업 확장 구조 | engagement 모듈 |
| `docs/06_외국인력_비자_및_공급전략.md` | 체류자격별 취업 가능 여부, F-4 §4 판단 근거 | 매칭 비자 하드 필터 |
| `docs/07_품질관리_및_리스크통제.md` | 클리어런스 6종, 업무범위 게이트, 교대 설계 | quality 모듈 |
| `docs/08_리크루팅_전략_및_D10_실행안.md` | 세그먼트별 채널, 코호트 운영 | recruiting 모듈 |
| `docs/09_IA_및_디자인시스템.md` | 메뉴 구조, 디자인 트랙 2종, 토큰 원본 | `packages/ui` |
| `docs/12_리스크_레지스터_및_의사결정로그.md` | **단계별 착수 가능 여부 판정** | 착수 판단 |
| `docs/05`, `docs/10` | 시장조사 · 유닛이코노믹스 | (판단 근거) |
| `design/*.dc.html` 8종 + `support.js` | 화면별 확정 수치·카피 (인라인 스타일이 곧 스펙) | 화면 구현 정밀도 |

## 3. 자료 간 불일치 — 확정 필요

취합 과정에서 확인된 충돌입니다. 구현 전에 어느 쪽이 기준인지 정해야 합니다.

| # | 항목 | 충돌 |
|---|---|---|
| 1 | **화면 수** | `CLAUDE.md` §0 "36개 화면" · §2 "V1 27 + V2 13 + V3 1 = 41" · `docs/01` §10 "V1 24화면" · `design/README` "41 스크린" |
| 2 | **로케일** | `CLAUDE.md` §5.15 "한국어/베트남어/영어(ko·vi·en)" · `design/README` "ko/vi/ru/en 4개, FIELD는 4개 전부" — **ru(러시아어)가 디자인에만 존재** |
| 3 | **색 토큰** | `docs/09` §5.1 원본은 임상적 저채도(`#1F6F5C`) · `design/README`가 밝은 블루(`#2F6BFF`)로 대체 확정. **09 원본 §5.1 덮어쓰기 필요** |
| 4 | **앱 구성** | `CLAUDE.md` §4는 `apps/mobile-caregiver`를 V2로 · `design/README`는 Caregiver App(SCR-401~405)을 FIELD 트랙에 포함 |

## 4. 착수 전 법무·노무 선행 항목

`docs/01` §13 기준. **개발의 90%는 지금 착수 가능하며, 막힌 것은 `DirectEmploymentStrategy`의 급여 계산 로직 하나입니다.**

| # | 질문 | 막히면 |
|---|---|---|
| A | 기관 현장 배치가 도급인가 근로자파견인가 | 사업 구조 전체 |
| B | 파견 허용 여부 / 근로자공급사업 허가 필요 여부 | 계약서, 기관 영업 |
| F | **F-4 자격자의 간병 업무 종사 가능 여부** | 55만 명 모수 채널 상실 (시드에 `PENDING_CONFIRMATION`으로 반영됨) |
| G | 급여화 공동간병 수가 | 요양병원 채널 고용모델 결정 불가 |
| H | 개인정보 국외이전 동의·계약 | 현지 파트너 접촉 자체가 위법 |

## 5. 다음 단계

```
1. wireframe/index.html (SCREENS) + docs/02·04·06~10·12 추가        ← 지금 대기 중
2. §3 불일치 4건 확정
3. 모노레포 스캐폴딩 (CLAUDE.md §4 구조)
4. 스키마 마이그레이션 + 시드 (industries/tracks/track_requirements)  ← CLAUDE.md §8-1
5. iam → talent → org → matching → ops  (§8 순서 그대로)
```
