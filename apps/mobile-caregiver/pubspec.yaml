name: carelink_caregiver
description: CARELINK 간병사 앱 (SCR-401~404) — FIELD 트랙
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # 토큰·API 클라이언트·위젯은 후보자 앱과 공용입니다 (packages/field_ui).
  carelink_field_ui:
    path: ../../packages/field_ui
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
