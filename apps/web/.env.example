# 서버 컴포넌트가 부르는 API. 컨테이너 밖에서 접근할 주소가 아니라
# Next 서버가 볼 수 있는 주소여야 합니다.
CARELINK_API_URL=http://127.0.0.1:3000/api/v1

# 로그인 화면(브라우저)이 OTP 발송에 직접 부르는 주소.
# 토큰은 여기로 오가지 않습니다 — /api/session이 받아 httpOnly 쿠키에 넣습니다.
NEXT_PUBLIC_API_URL=http://127.0.0.1:3000/api/v1
