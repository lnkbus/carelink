/**
 * 근로자파견 기간 제한 (파견법 §6).
 *
 * 동일 사용사업주에게 동일 근로자를 **2년 초과** 파견할 수 없습니다.
 * 초과하면 사용사업주에게 **직접고용 의무**가 발생합니다 — 위반 시 과태료가
 * 아니라 고용 관계가 강제로 성립하므로, 기관 입장에서는 계약 구조가 통째로
 * 뒤집힙니다.
 *
 * 상수로 두는 이유: 이 값은 법에서 오고 운영자가 조정할 수 없습니다.
 * `matching_rules`처럼 테이블에서 읽으면 누군가 늘릴 수 있게 되고,
 * 늘리는 순간 불법이 됩니다 (§5.5의 예외입니다).
 */
export const DISPATCH_LIMIT_DAYS = 730;

/** 사전 경고 시점. 2단계로 돌립니다 — 대체 인력 확보에 시간이 걸립니다. */
export const DISPATCH_WARN_DAYS = [90, 30] as const;
